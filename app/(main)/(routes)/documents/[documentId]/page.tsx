/* eslint-disable */

interface DocumentIdProps {
  prop: string;
}

const DocumentId = ({
  prop,
}: DocumentIdProps) => {
  console.log();
  return (
    <div>
      page
    </div>
  );
}

export default DocumentId;
