interface DocumentsPageProps {
  prop: string;
}

const DocumentsPage = ({
  prop,
}: DocumentsPageProps) => {
  console.log(prop);
  return (
    <div>
      {' '}
    </div>
  );
};

export default DocumentsPage;
